/** Max upload size for video moments (browser direct-to-S3 via presigned PUT). */
export const MAX_VIDEO_UPLOAD_BYTES = 1024 * 1024 * 1024; // 1 GiB

export const MAX_AUDIO_UPLOAD_BYTES = 40 * 1024 * 1024; // 40 MB
export const MAX_IMAGE_UPLOAD_BYTES = 40 * 1024 * 1024; // 40 MB

export const MAX_VIDEO_UPLOAD_LABEL = '1 GB';
